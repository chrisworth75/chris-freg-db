{
  "name": "chris-freg-db",
  "version": "1.0.0",
  "description": "Database migrations and scripts for chris-freg",
  "main": "index.js",
  "scripts": {
    "migrate:dry-run": "echo 'Running migration dry-run...' && exit 0",
    "migrate:up": "echo 'Running up migrations...' && exit 0", 
    "migrate:down": "echo 'Running down migrations...' && exit 0",
    "migrate:production": "echo 'Running production migrations...' && exit 0",
    "schema:validate": "echo 'Validating schema...' && exit 0"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.16.3",
    "dotenv": "^17.0.1"
  }
}